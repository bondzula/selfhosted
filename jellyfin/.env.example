# General
TZ=Europe/Belgrade

# Jellystat Database Configuration
JELLYSTAT_POSTGRES_DB=jfstat
JELLYSTAT_POSTGRES_USER=jellyfin_user
# Generate a strong password: openssl rand -base64 32
JELLYSTAT_POSTGRES_PASSWORD=change_this_to_secure_password

# Generate a secure JWT secret: openssl rand -base64 32
JELLYSTAT_JWT_SECRET=change_this_to_secure_jwt_secret
